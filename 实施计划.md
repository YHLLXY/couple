# 情侣心愿小程序 —— 实施计划书

> 面向 DeepSeek Coding Agent 的工程化实施文档
> 文档版本：v1.0 | 适用项目：移动端情侣心愿小程序（暂定名「小甜豆」）

---

## 一、项目愿景与核心定位

### 1.1 一句话定位
**「让对方知道你今天想被宠爱」** —— 一个让情侣每天写下"希望对方为自己做的事"，并彼此响应、点亮甜蜜日常的移动端应用。

### 1.2 核心场景
- 小A早起写下今天的小心愿："想吃你做的番茄炒蛋"。
- 小B在自己的 App 上收到推送，进入 App 后看到心愿，可以点击"我来完成"或"留言回应"。
- 完成后打卡上传（可加一张照片），双方均可看到。
- 留下每天一点点的甜。

### 1.3 设计原则
1. **轻、暖、可拆**：移动端首屏渲染 < 1.5s，UI 永远粉嫩，每个功能模块独立可拆。
2. **功能可插拔**：所有模块通过统一的"功能注册中心"挂载，禁用某模块不影响其它功能。
3. **数据双端同步**：本地优先 + 云端同步，断网也能用。

---

## 二、技术栈选型（推荐方案）

### 2.1 推荐主栈：**Vue 3 + Vite + Vant 4 + Pinia + TypeScript**
理由：
- Vant 是移动端 H5 标杆 UI 库，已内置大量情侣场景会用到的卡片、弹窗、标签组件。
- Vue3 Composition API 适合做"模块化即插即拔"。
- TS 保证后期扩展不出现类型黑洞。
- 最终可打包成 **微信小程序**（用 uni-app / Taro 跨端），或纯 H5 网页（用 PWA 加壳）。

### 2.2 备选栈
- **React + Vite + Ant Design Mobile**：团队熟悉 React 时选这个。
- **纯微信小程序原生**：仅需微信生态时选这个，性能最好但跨端差。

### 2.3 数据后端（轻量化方案）
- MVP 阶段：**Mock + LocalStorage**，用 `src/mocks/` 提供假数据。
- 中期：**微信云开发 / CloudBase**，无需搭服务器。
- 长期：**Supabase / 自建 Node 后端**。

### 2.4 移动端兼容性策略
- 使用 `postcss-px-to-viewport` 或 `lib-flexible` 做 REM 适配。
- 最低兼容 iOS 12 / Android 7。
- 不使用未加 polyfill 的 ES2020+ 语法（开发时开启 `@vitejs/plugin-legacy`）。

---

## 三、模块化架构（核心骨架）

整个 App 采用「**插件化功能模块**」架构。每个功能模块是独立的文件夹，包含完整的「数据 + 视图 + 状态 + 路由」。

### 3.1 模块清单（按优先级）

| 模块 ID | 模块名 | 优先级 | 是否核心 |
|---|---|---|---|
| `core` | 核心壳（启动、布局、注册中心） | P0 | ✅ |
| `user` | 用户与情侣绑定 | P0 | ✅ |
| `wish` | 心愿墙（核心） | P0 | ✅ |
| `interact` | 互动（点赞、贴纸、留言） | P1 | ✅ |
| `calendar` | 甜蜜日历 | P1 | ✅ |
| `reward` | 积分/甜蜜值 | P2 | 可选 |
| `diary` | 共同日记 | P2 | 可选 |
| `theme` | 主题与个性化 | P1 | ✅ |
| `notify` | 通知与提醒 | P1 | ✅ |

### 3.2 模块目录结构（每个模块统一）

```
src/modules/<module-name>/
├── index.ts          # 模块导出 + 注册信息
├── routes.ts         # 路由声明
├── store.ts          # Pinia store（独立命名空间）
├── components/       # 模块私有组件
├── views/            # 页面
├── api.ts            # 数据接口（请求/Mock）
├── types.ts          # 类型定义
└── README.md         # 模块说明
```

### 3.3 功能注册中心（关键）

```ts
// src/core/registry.ts
export interface ModuleManifest {
  id: string;
  name: string;
  icon?: string;        // TabBar 图标
  routes: RouteRecordRaw[];
  store?: PiniaStoreDefinition;
  tabBar?: boolean;     // 是否在底部 Tab 出现
  enabled: boolean;     // 总开关
}

export const moduleRegistry: ModuleManifest[] = [];
export function registerModule(m: ModuleManifest) { ... }
export function unregisterModule(id: string) { ... }
```

**禁用某模块 = 把它的 `enabled` 设为 false，App 启动时跳过路由与 Tab 注册即可，零侵入。**

---

## 四、核心功能详细设计

### 4.1 用户与情侣绑定模块（`user`）

#### 功能点
- 微信一键登录 / 手机号注册。
- 个人资料：昵称、头像、昵称（对内称呼，比如"老公/老婆"）、生日。
- **情侣绑定流程**：
  - 生成专属邀请码（6 位数字 + 字母）。
  - 扫描对方二维码 / 输入邀请码绑定。
  - 绑定成功播放一段小动画（两颗心相撞 → 合并）。
- 解绑 / 重新绑定（带二次确认）。

#### 关键交互细节
- 绑定成功后**首页 Banner 变为对方头像**，点击头像可查看对方主页。
- 解绑前弹出"分手警告"，二次确认必须停留 3 秒才能点击。

#### 数据结构
```ts
interface User {
  id: string;
  openId?: string;
  nickname: string;
  avatar: string;
  partnerId?: string;
  coupleCode?: string;
  createdAt: number;
}
```

---

### 4.2 心愿墙模块（`wish`）—— **最核心**

#### 功能点
1. **发心愿**：输入文字（≤50 字），可选分类标签、优先级（普通/小急/小浪漫）、是否匿名、是否附图。
2. **看心愿**：分两栏 Tab ——「TA 给我的」/「我给 TA 的」。
3. **响应心愿**：四个动作
   - 🤗 **接单**（我来完成）
   - 💬 **留言**（聊聊）
   - ⏰ **延期**（今天做不了，明天做）
   - 💕 **小惊喜**（已完成 + 附一张照片/一句话证明）
4. **心愿状态机**：`待响应 → 已接单 → 已完成 / 已延期 / 已忽略`
5. **每日上限**：默认每人每天可发 5 条（可在设置里改）。
6. **模板库**：内置 50+ 模板（如"帮我带一杯奶茶""今晚一起看电影"），点一下即可发。

#### 视觉与交互
- 心愿卡：圆角 16px，浅粉渐变背景，右上角状态徽章。
- 长按心愿 → 弹出"复制 / 删除 / 撤回"。
- 接单后卡片微微震动 + 弹出小动效。
- 已完成心愿进入"甜蜜墙"（双方共通的回忆墙）。

#### 数据结构
```ts
type WishStatus = 'pending' | 'accepted' | 'done' | 'postponed' | 'ignored';
type WishCategory = 'food' | 'chore' | 'romance' | 'company' | 'surprise' | 'other';

interface Wish {
  id: string;
  fromUserId: string;
  toUserId: string;
  content: string;
  category: WishCategory;
  priority: 'normal' | 'urgent' | 'romantic';
  status: WishStatus;
  imageUrl?: string;
  proofImageUrl?: string;     // 完成时的证据图
  proofNote?: string;
  createdAt: number;
  completedAt?: number;
  expireAt?: number;          // 当天 23:59 过期
}
```

---

### 4.3 互动模块（`interact`）

#### 功能点
- **点赞 / 比心**：每条心愿、每条日记都可点赞（小红心漂浮动画）。
- **贴纸回复**：内置 30 个甜蜜贴纸（抱抱、亲亲、生气、想你…），点击贴纸即可回应。
- **悄悄话**：单独通道，只能一对一发送一次性的"限时消息"，24h 后消失（类似阅后即焚）。
- **甜蜜签到**：每天点一下"今天也想你"，对方会收到签到提醒。

#### 关键细节
- 贴纸回复后，对方屏幕**轻轻晃一下** + 贴纸飘入聊天气泡。

---

### 4.4 甜蜜日历模块（`calendar`）

#### 功能点
- 月历视图：每一天是一个小方格。
  - **绿色**：双方都签到。
  - **粉色**：有未完成的心愿。
  - **黄色**：当天有纪念日。
- 点击某天 → 进入"那天我们"的详情页：当日心愿列表、当日贴纸、当日日记。
- **纪念日管理**：可手动添加纪念日（在一起、第一次接吻、第一次旅行…），App 自动倒计时。倒计时 7 天内的纪念日在首页弹气泡提醒。

---

### 4.5 积分与甜蜜值模块（`reward`）—— 可选

#### 规则
- 完成 1 个心愿 = +5 甜蜜值
- 对方点赞 = +1
- 连续 7 天签到 = +10 + 解锁成就「一周甜蜜」
- 甜蜜值满 100 → 解锁"默契奖牌"，可自定义勋章墙。

#### 为什么不强制
- 部分情侣不喜欢"打分制"，所以**默认关闭**，在设置里开关。

---

### 4.6 共同日记模块（`diary`）—— 可选

- 双方共写一篇日记，每人每日一段（最多 200 字）。
- 支持添加当日的照片（最多 9 张）。
- "我们的第一篇日记"自动写好开篇模板。

---

### 4.7 主题与个性化模块（`theme`）

- 内置 3 套主题：
  - 🍓 **草莓粉**（默认）：粉嫩、少女
  - 🫐 **蓝莓蓝**：清新、少年感
  - 🍋 **柠檬黄**：明亮、温暖
- 字体大小、圆角大小、动效强度均可调节。
- 设置统一存 `localStorage.themeConfig`，并通过 CSS 变量实时响应。

---

### 4.8 通知模块（`notify`）

- 心愿状态变更时，触发本地通知（移动端用 `plus.push` 或 Web Push）。
- 每天 21:00 推送"今天还有未完成的心愿哦~"（可在设置关闭）。

---

## 五、UI / UX 设计规范

### 5.1 配色方案（默认主题：草莓粉）

| 用途 | 颜色名 | 色值 | 备注 |
|---|---|---|---|
| 主色 Primary | 草莓粉 | `#FF7A95` | 按钮、强调 |
| 辅色 Secondary | 蜜桃橙 | `#FFB17A` | 渐变、Tag |
| 强调 Accent | 薄荷绿 | `#7AE0C4` | 成功状态 |
| 背景 Background | 奶油白 | `#FFF8F2` | 页面底色 |
| 卡片 Card | 樱花粉 | `#FFEEF3` | 卡片底色 |
| 文字主 | 深咖 | `#4A3A3A` | 正文 |
| 文字次 | 浅咖 | `#A89A9A` | 辅助 |
| 危险 Danger | 草莓红 | `#FF5A5A` | 删除、警告 |

**渐变建议**：`linear-gradient(135deg, #FF7A95 0%, #FFB17A 100%)`

### 5.2 字体
- iOS：`PingFang SC`，Android：`HarmonyOS Sans / 思源黑体`，英文：`Quicksand`（圆润）。
- 默认大小 14px，标题 18-22px，价格/数字用粗体强调。
- 圆角统一：组件 8px、卡片 16px、按钮 999px（胶囊）。

### 5.3 动效
- 进入动画 200ms ease-out。
- 心愿接单时卡片轻微 "shake" + 缩放。
- 点赞时心形从下往上飘 800ms。
- 全局禁用超过 400ms 的长动画（移动端流畅优先）。

### 5.4 图标与吉祥物
- 吉祥物：**粉色兔少女（她）+ 蓝色兔少年（他）**，详见 `设计素材/` 目录。
- App 图标：心形 + 兔兔剪影，详见 `设计素材/`。
- 所有图标统一用线性 2px 描边，圆角端点。

---

## 六、整体目录结构

```
couple-wish-app/
├── public/
│   └── assets/                  # 静态资源
├── src/
│   ├── core/                    # 核心壳
│   │   ├── registry.ts
│   │   ├── layout/              # 全局布局（TabBar、Header）
│   │   └── http/                # 请求封装
│   ├── modules/
│   │   ├── user/
│   │   ├── wish/
│   │   ├── interact/
│   │   ├── calendar/
│   │   ├── reward/              # 可选
│   │   ├── diary/               # 可选
│   │   ├── theme/
│   │   └── notify/
│   ├── components/              # 全局通用组件
│   │   ├── HeartButton.vue
│   │   ├── WishCard.vue
│   │   ├── EmptyState.vue
│   │   └── ...
│   ├── styles/
│   │   ├── tokens.css           # 设计变量（色、尺寸、阴影）
│   │   ├── reset.css
│   │   └── animations.css
│   ├── stores/                  # 全局 store（如 userInfo）
│   ├── utils/
│   ├── mocks/                   # 假数据
│   ├── App.vue
│   └── main.ts
├── 设计素材/                    # 已生成的 UI 素材
│   ├── 情侣吉祥物.png
│   ├── App图标.png
│   └── 装饰插画.png
├── docs/
│   └── 实施计划.md              # 本文档
├── index.html
├── vite.config.ts
├── tsconfig.json
└── package.json
```

---

## 七、给 DeepSeek Coding Agent 的实施建议

> 这一节专门写给后续接手开发的 DeepSeek，请**严格遵循**。

### 7.1 工作方式
1. **模块独立交付**：每次任务只交付一个完整模块，先跑通 mock 数据，再接真实 API。
2. **先骨架后血肉**：先把目录、路由、注册中心搭起来，再逐个填充模块。
3. **保持 TS 严格模式**：`strict: true`，禁止 `any`（除非有注释说明）。
4. **不要偷工减料**：组件库尽量用 Vant 提供的，避免重复造轮子；但**样式必须按本规范覆盖**。

### 7.2 单模块开发流程（标准 SOP）
1. 在 `src/modules/<name>/` 建好目录。
2. 定义 `types.ts`（所有数据结构）。
3. 实现 `api.ts`（先返回 mock 数据）。
4. 实现 `store.ts`（Pinia）。
5. 实现 `views/` 和 `components/`。
6. 在 `core/registry.ts` 注册模块。
7. 自测：模拟数据渲染、TabBar 切换、禁用该模块后其它模块仍正常。
8. 在 `docs/CHANGELOG.md` 写一行记录。

### 7.3 禁用模块的"零侵入"验证
- 每完成一个模块，都要测试：**在注册中心关闭它**，确认其他模块正常运行。
- 这是项目可扩展性的核心。

### 7.4 性能红线
- 首屏 JS bundle < 250KB（gzipped）。
- 路由懒加载（`() => import(...)`）。
- 心愿列表用虚拟滚动（`@vant/touch-emulator` 或 `vue-virtual-scroller`），超过 50 条时启用。
- 图片用 `<img loading="lazy">`，并提供占位图（粉色渐变 + 模糊）。

### 7.5 代码风格
- ESLint + Prettier + Vue 官方风格。
- 命名：组件 PascalCase，方法/变量 camelCase，常量 UPPER_SNAKE_CASE。
- 每个 `.vue` 文件 `setup` + `<script setup lang="ts">`。
- 提交信息格式：`feat(wish): 新增心愿状态切换`

### 7.6 测试
- 至少为 `wish` / `user` / `reward` 三个核心模块编写 Vitest 单测。
- 关键工具函数（如时间格式化、状态机切换）100% 覆盖。

### 7.7 不要做的事
- ❌ 不要使用 jQuery。
- ❌ 不要引入未在本文档列出的重型库（如 Element Plus、Ant Design 全家桶）。
- ❌ 不要在没有 type 标注的情况下写接口。
- ❌ 不要在 `core/` 之外直接写 localStorage —— 全部走 `core/storage.ts` 封装。
- ❌ 不要写死颜色值 —— 全部走 `var(--xxx)`，从 `tokens.css` 读取。

---

## 八、阶段交付计划

### 阶段 1：骨架搭建（预计 1 天）
- 初始化 Vite + Vue3 + TS + Vant 项目。
- 实现 `core/` 模块、注册中心、TabBar 框架、主题切换。
- 占位所有模块路由。
- **交付物**：能跑起来的空壳 App，TabBar 切换正常。

### 阶段 2：核心功能 MVP（预计 3 天）
- `user`：登录、绑定（先 mock）。
- `wish`：发心愿、看心愿、接单、完成。
- `notify`：本地通知。
- **交付物**：能完整跑通"小 A 发心愿 → 小 B 接单 → 小 A 确认完成"流程。

### 阶段 3：互动增强（预计 2 天）
- `interact`：点赞、贴纸、签到。
- `calendar`：日历视图。
- `theme`：完善主题切换。

### 阶段 4：可选与扩展（预计 2 天）
- `reward`：积分系统。
- `diary`：共同日记。
- 接入真实后端（替换 mock）。

### 阶段 5：打磨与上架（预计 1-2 天）
- 移动端真机适配、iPhone 安全区、刘海屏处理。
- PWA 配置 / 微信小程序跨端适配。
- 性能优化、上线。

---

## 九、扩展性设计 —— 后续如何加新功能？

只需 4 步：

1. 在 `src/modules/<new-feature>/` 创建新模块（按 §3.2 规范）。
2. 在 `core/registry.ts` 添加一行注册。
3. （可选）在 `theme` 中加配色。
4. 完成。

**无需改动其它任何模块。**

例如后续要加"情侣游戏"模块？建一个 `modules/game/` 即可。

---

## 十、UI 素材清单

已生成（详见 `设计素材/` 目录）：

| 文件 | 用途 |
|---|---|
| `A_pair_of_cute_couple_mascot_*.png` | **情侣吉祥物**（粉兔女 + 蓝兔男），用于启动页、关于页、空状态插画 |
| `A_cute_mobile_app_icon_design_*.png` | **App 图标**，可用于应用图标、关于页、欢迎页 |
| `A_cute_kawaii_decorative_illus_*.png` | **顶部装饰插画**，可用于首页 Banner、引导页背景 |

> 后续还需要更多素材（图标、贴纸、空状态插画、纪念日图标等）时，可继续使用 image_gen 生成，prompt 模板见 §附录。

---

## 附录 A：image_gen Prompt 模板（供后续生成更多素材）

```
风格统一提示词前缀（建议每次追加）：
"kawaii flat vector style, soft pastel candy palette, mobile app illustration, 
clean outlines, minimal shading, high quality, cute, suitable for couple app"
```

按用途分类：

- **图标类**（如分类图标、功能图标）：
  `"a tiny kawaii icon of <物体>, line art 2px stroke, rounded corners, pink theme, 
  suitable for mobile UI icon set"`
- **空状态插画**：
  `"a cute kawaii illustration of <场景>, showing an empty state, 
  two chibi bunny characters looking a little sad, soft pink, encouraging mood"`
- **成就徽章**：
  `"a cute circular achievement badge, kawaii style, pink and gold theme, 
  with <主题> icon in the center, suitable for couple app reward system"`
- **表情/贴纸**：
  `"a kawaii chibi bunny sticker making <表情> expression, transparent background, 
  pink theme, suitable for messaging app sticker pack"`

---

## 附录 B：状态机图（心愿模块）

```
        ┌──────────┐
        │ pending  │ (待响应)
        └────┬─────┘
             │
   ┌─────────┼─────────┬──────────┐
   ▼         ▼         ▼          ▼
accepted  postponed  ignored      done
(已接单)   (已延期)   (已忽略)    (已完成)
   │                   │
   └─────► done ◄───────┘
```

---

**文档结束。后续按本计划逐步落地，每个模块完成后请更新 §十 的素材清单与 §八 的阶段勾选状态。**